# Google OAuth 2.0 Configuration
# 获取这些值：https://console.developers.google.com/

# === 开发环境配置 (当前使用) ===
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
REDIRECT_URL=http://localhost:8081/auth/callback

# 可选：环境标识
ENVIRONMENT=development
PORT=8081
APP_SERVER_URL=http://localhost:8082

# === 生产环境配置示例 (未来使用) ===
# GOOGLE_CLIENT_ID=your_production_client_id
# GOOGLE_CLIENT_SECRET=your_production_client_secret
# REDIRECT_URL=https://yourdomain.com/auth/callback
# ENVIRONMENT=production
# PORT=443
# APP_SERVER_URL=https://yourdomain.com

# 注意：
# 1. 开发环境：localhost 完全可以，Google OAuth 支持
# 2. 生产环境：需要真实域名 + HTTPS
# 3. 需要在 Google Console 中为每个环境单独配置 OAuth 客户端
